{
  "name": "911-devops",
  "version": "1.0.0",
  "description": "Emergency DevOps monitoring and recovery system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "bot": "node bot.js",
    "lint": "eslint . --ext .js --ignore-pattern node_modules/ --ignore-pattern coverage/",
    "lint:fix": "eslint . --ext .js --ignore-pattern node_modules/ --ignore-pattern coverage/ --fix",
    "test": "jest --verbose",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@slack/bolt": "^3.17.0",
    "docker-modem": "^5.0.6",
    "dockerode": "^4.0.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
