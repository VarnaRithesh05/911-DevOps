# IMPORTANT: This is a template file. DO NOT commit actual secrets to git!
# Create secrets using kubectl commands instead:
# kubectl create secret generic slack-secrets \
#   --from-literal=bot-token=xoxb-YOUR-BOT-TOKEN \
#   --from-literal=app-token=xapp-YOUR-APP-TOKEN

apiVersion: v1
kind: Secret
metadata:
  name: slack-secrets
  labels:
    app: devops-911
type: Opaque
stringData:
  bot-token: "REPLACE_WITH_YOUR_SLACK_BOT_TOKEN"
  app-token: "REPLACE_WITH_YOUR_SLACK_APP_TOKEN"
